# GLMath
A C++ header-only library that implements useful functions for common math operations used in OpenGL.

Features:
 - Custom vector and matrix sizes.
 - Custom types too!

To be added:
- Perspective matrix function
- Orthogonal matrix function
